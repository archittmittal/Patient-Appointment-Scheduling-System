/**
 * Centralized Constants
 * Defines fallback values and default system limits.
 */
module.exports = {
    /** Default predicted consultation duration (minutes) — used as fallback in booking and analytics. */
    DEFAULT_PREDICTED_DURATION: 15,
    /** Default max patients per slot when doctor profile field is missing. */
    DEFAULT_MAX_PATIENTS_PER_SLOT: 15,
};
