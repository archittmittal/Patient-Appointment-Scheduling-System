package com.hospital.model.enums;

/** Appointment type classification. Maps to: appointments.appointment_type ENUM */
public enum AppointmentType {
    NEW, FOLLOW_UP, CHECKUP, EMERGENCY, CONSULTATION
}
