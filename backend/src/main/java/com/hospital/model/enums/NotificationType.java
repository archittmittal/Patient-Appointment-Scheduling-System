package com.hospital.model.enums;

/** Notification type. Maps to: notifications.notification_type ENUM */
public enum NotificationType {
    BOOKING_CONFIRM, REMINDER, QUEUE_UPDATE, CANCELLATION, NOSHOW_WARNING, RESCHEDULE
}
